bind grades = [3.5, 4.0, 3.8]

repeat g in grades:
    emit "Grade: " + g

bind nums = []

append(nums, 10)
append(nums, 20)

append(nums, 4.0)
emit len(nums)
emit nums

append(grades, nums)

emit DEBUGGER